// Components/ProjectPage.js
import React from 'react';

const ProjectPage = ({ projectTitle }) => {
    return (
        <div>
            <h1>Welcome to the {projectTitle} Project Page!</h1>
        </div>
    );
};

export default ProjectPage;
